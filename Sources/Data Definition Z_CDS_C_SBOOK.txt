@AccessControl.authorizationCheck: #NOT_REQUIRED
@Metadata.allowExtensions: true
@EndUserText.label: 'Projection view of Z_CDS_I_SBOOK'
@Metadata.ignorePropagatedAnnotations: true
define root view entity Z_CDS_C_SBOOK
provider contract transactional_query as projection on Z_CDS_I_SBOOK
{
    key AirlineID,
    key ConnectionID,
    key FlightDate,
    key BookingID,
    PassengerName,
    Cancelled
}
